# Outer implementation and review

The owner-authorized Independent Application Acceptance V0 plan in the Capy
control repository governs Astra integration and independent read-only reviews.
The bounded Muse experiment ended after two preserved runs. Astra now owns
product integration, durability, CLI, packaging, qualification and closure.
Developer, DevKit and runtime implementation repositories remain read-only.
Do not run another Muse contribution, change the original oracle bytes, publish
a package or deploy. Keep source repairs separately attributable after Muse.

The following boundary applied to the frozen Muse task packet. It remains the
rule for any contributor environment, not a restriction on owner-authorized
outer Git, qualification and independent review work.

## Contributor boundary

This is a public synthetic repository. Read only this workspace and its frozen task packet. Do not access parent directories, sibling repositories, personal context, credentials, sessions, or the developer home directory.

Do not use network research or shell network access. Do not spawn subagents. Do not disable the sandbox. Do not commit, push, publish, merge, or deploy.

Before source work, an explicit frozen task packet must name writable paths. Until then this repository is preflight-only. Do not edit existing specifications, tests, fixtures, task packets, or Git metadata. The outer orchestrator owns Git and qualification. Report facts without claiming product acceptance.
