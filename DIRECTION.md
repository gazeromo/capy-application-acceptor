# Direction

One copied candidate plus one independently frozen profile yields an independently validated accepted receipt or causal rejection. Runtime integration is outside this repository. Production package code must have zero model-provider dependency.
