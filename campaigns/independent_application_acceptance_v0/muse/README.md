# Bounded Muse Contributor experiment

Run1 exact untouched commit fec88e377a12039826a5afa3388b8bb3dd7d4b64;
Run2 exact untouched commit15075306a3de409e4e33666680466f3076ced3a6.
Astra made only mechanical snapshot commits. First-pass visible25/25 and
hidden62/65; after one bounded repair visible40/40 and hidden65/65.
Original hidden bytes are preserved under ../oracle-original with frozen hashes.

Run1 had fiveP1/twoP2 independent source findings, separately recorded in
RUN-1-REVIEW.md. Run2 source review and final retained-code assessment follow.
A separate post-packet .gitignore integration finding was not fed to Muse.
A final grade requires source-review findings and measured Astra changes;
test passes alone are not product acceptance or a general model-quality claim.

Requested and observed model muse-spark-1.3-contributor; official Muse Code
1.0.3-R2198.1 binary unchanged. Both runs completed normally. Zero nested agents,
zero scope violations, zero advice during Run1, and only the frozen packet
before passiveRun2. Real provider credential stayed in a separate sidecar and
was discarded when its container ended after both preserved exports.

Visible trajectory files contain only observable tool facts, command text,
output hashes and final summaries. Full redacted exports remain local because
they retain encrypted reasoning. The CLI durable export reports live-only
omissions; counts and export hashes are disclosed. No private reasoning claim.
Provider usage totals are reported as exposed; cache fields overlap and cost
was not exposed. Native-host failed preflights are preserved separately and
are not implementation-run failures or extra coding attempts.
